fastapi==0.115.0
uvicorn[standard]==0.30.6
kubernetes==30.1.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
# Pinned: the unconstrained resolution pulled cryptography==50.0.1,
# whose compiled Rust extension SIGILLs (exit 132) on this host's
# virtualized arm64 CPU - reproduced by bisecting a crash all the way
# down to `google.auth.transport.requests` -> `google.auth.crypt.es` ->
# `cryptography.exceptions` (loaded transitively via kubernetes/config,
# even though this app never does GKE/service-account JSON auth).
# 42.0.5 is a long-established release with none of these reports.
cryptography==42.0.5
